# Course 3 : Machine Learning : Module 2 : Concepts and Principles

**Sources:** 9 | **Exported:** July 14, 2026

---

## 1. Fundamentals of ML Study Guide - M2.pdf

### Summary

This educational guide provides a foundational overview of how **machine learning** operates within a clinical context, specifically by contrasting it with traditional **rules-based programming**. The text details the mechanics of **supervised learning**, explaining how models utilize **training, validation, and test datasets** to discover mathematical functions that map input features to labeled outputs. It further explores the practical application of various algorithms—such as **regression, decision trees, and support vector machines**—while addressing the differences between **structured and unstructured data** representation. Ultimately, the source serves to clarify essential terminology and the **iterative training loops** required to develop predictive tools that can assist in medical decision-making.

**Keywords:** Supervised learning, Machine learning paradigms, Model fitting procedure, Structured vs unstructured, Common ML approaches

### Content

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX_DnPSOMP0gPGJgwB6m9cgczLGchsXZKwJSr9lOtR5m6ukIIKB0bcWrcPXzjyq7OS3tI7bSBnlvIWMYCJ_UeG3x8TDsZll2phZTEaPeQgjPhTFnUf89nzlTlo4aPHNowsiuIiNbzA=w442-h129-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX8UpgMPTFxIB2rGrtOPZLeW68Gt3PvygDoEZrTg1QNQL5kWP73wIc4vYAfxh88ZQl2j6gNKfdcO-BKG_fbHlLq_nxWhg2RVHgqTksd7pMIdY4Lsl8oeKc5AClGPJ0Rr_xDlgIqJsA=w600-h73-v0)

 FUNDAMENTALS OF MACHINE LEARNING FOR HEALTHCARE MODULE 2 - CONCEPTS AND PRINCIPLES OF MACHINE LEARNING IN HEALTHCARE PART 1 LEARNING OBJECTIVES Distinguish the machine learning subfield from other areas of artificial intelligence and computer science. Describe the model fitting procedure in the supervised learning setting and distinguish supervised learning from unsupervised learning in healthcare applications. Understand the difference between structured and unstructured data, as well as some of the commonly used methods to represent unstructured data. Become familiar with common machine learning approaches like regression, support vector machines, and decision trees and how they might apply to clinical problems. MACHINE LEARNING TERMS, DEFINITIONS, AND JARGON Definitions of Machine Learning Formal: A family of statistical and mathematical modeling techniques that uses a variety of approaches to automatically learn and improve the prediction of a target objective, without explicit programming Informal: Systems that improve their performance in a given task, through exposure to experience, or data Three machine learning paradigms Supervised Learning Unsupervised Learning Reinforcement Learning Machine learning problems fall along a spectrum of supervision between these terms. [Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX9V9tOqT72LdswHAP8s6bhmQWJAQerOPON0866W9lyOtxvgySTCbSZq7YEp9zuRiEs0MztMJIhDP2wPu2QSiu6zNG1nQ5RA8lc6BEzfNcjBIWucvA_a18GAtnNsQoj-VfZlgspPsw=w134-h39-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX8UAPFkPzZKCOBACgYlGa-NSgv_7IlsVa9nLb7iwTTKHEpI7EDBeQQ1G0z6mKvpIFsAYEunWPM7dsCZ0sxIjMLCl1zyWSl1yFnV5rjC39GgNIi5zMPNJLd-DzV9OEBaJ-b3O3pFCA=w664-h32-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX-qVfy-_l4T2DnUh7i_qabqbFkSoSXMejpDaucVnF94oeNhGHvSNhQQoKxfWTNUwSqhCOsv7fI22nkQgaPghRNRUifxwXQlGazFjUKfHhWsd5u-uUnLzP_DfM0120cftgNOMlNsPw=w664-h32-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX8xbLAOTw20oKmNDIzGiwNmuFBvHdwvRgxl4Cm55A5xi7peMvmvO9tpJur1bhI9aIUskiOgUAqB-XY0n-IxNbggO2m7zM4Au4HI0qo2_ZOTdZ_9ncXme7lORDUzLZel4cIYrT9P=w442-h129-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX_jnxTfSPq9bhoyJX4MEPg5Ks4q4zbTBgfn1vboBEYgtfaRSke_eFDpZF2RlPPQ2VJjB6MoVcf816C9fQszcqsKv3GID6aYk1fqp9fHIEC2WF1BV16DhPjx0oV5nUw3UQf3jb1g=w600-h73-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX-frV-jkOU_FEOjUsU5PD7kiOkjo5TjuHml-BupKVwCgFHWxkxlwmnjplcc3vz2epj00go4qi4lp8O0rS4_orDoeOACSKHnoVtD0kIYCeVRvA7WF_Kq1wlAeYKoTvoYmeEHnuBMOg=w1055-h572-v0)

 Explaining computer programming: Boils down to three components: (1) the input, (2) some processing, and (3) The output Example: Equation y = x^2. The input is x and an output y Abnormality detection. The input could be an ECG and the output could be a medical diagnosis like ST elevation myocardial infarction For both these examples, in between the input and the output, there is something that processes the input to produce the output. In example 1, it is squaring of the input x to arrive at the output y In example 2, there is a visual analysis being performed on the ECG leading to the output “Processing”, we are referring to is the thing that transforms the input into the output, is called a function. In a traditional computer programming approach, we deliberately write rules to process the inputs so that they produce the desired outputs. Traditional computer programming is also referred to as a “rules-based” approach. In other words, computer programmers write functions with specific rules - the program written is the function, or the processing, that the computer performs to achieve an output. Explaining Machine Learning, in particular supervised learning: [Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX-AIHUzxccLyUUB_4BM0IhO7HYan0WynXYEdzXhHQl-1RhijbboxyiOwRxkY0NcTOqnj4TvPinNnI11AYu42yeOH0fZ1KhFnD7bJFWjlgu8T3X-_cE-0Y8knZp1I4P6MDxJnEju1w=w442-h129-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX-ZkHAPse6_r4BJ459r_BqGMRC7LpUSxr6jGqTfZ8Q_UKRE00N3c32M04K5JaLYtSjCe2sdbPyMId-cPP0-9XyXV8U6GdqEhGmdtz4_9qulmyR7mVsP-9RZVbVRC_VfaNA-CO1frQ=w600-h73-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX_7_k2xrhvIs5DOzcVoddq-mL13lacjTHmsWoTl_JcNi1bSiycjNOxvuxJhiDkqXqMhlum6LVYl4XdkxvHE900x0XhvjAy4tma8ty1R1SKN6jv6oYOEYbiT3RKxXIzVoyiCmgXQwQ=w1182-h703-v0)

 The program written in this type of approach searches for (or in other terms, learns or finds) a function that can accurately map our data of inputs to outputs. We then use this function to process new inputs, and produce new outputs If a cardiologist has already looked at the ECG (the input) and recorded a diagnosis, (the output) then we likely have two parts to the equation. All we need to do is figure out the function that solves the input-output. Supervised learning is the process through which a program takes input-output pairs and ‘learns’ the function that links them together **Note that we call this ‘supervised’ learning because we provide input and output pairs. We are ‘supervising’ the model by providing it with the right answers. **The model– the entity that undergoes supervised learning It ‘represents’ or ‘models’ the relationships between the inputs and the outputs Learning this relationship means learning a function which, in this case, means adjusting a set of numbers known as parameters A model is defined entirely by its parameters and the operations between them Sometimes called a model a function approximator– it approximates the function between the inputs and the outputs Once the program learns a function that works well, we can use it in place of software that would have been written by traditional computer programming. We can take new inputs, put them [Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX9hxaEQHOwgPwpf7IZchqo7BFBZRGT16wMroeXCBQq8iJ6FjK0mpkdSS0IOM5ozDPCOqQseKr5IkQRD-JKD_X0dV9zs3ix1ki9mYlPUXNtENo9uy000qM_9PBh1CgD-mT1JG_vavA=w442-h129-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX9gy5mJghZNKYmibdwOgz1p05jNINFMwvotuD-7BQz7aeyMkmhWmsLYnFR1TU9PVAtW4rZIOJQi_OGueTuYXzuM4I5ov3NjlUj6W0RJSCyUOwuk5225GGlaxgdsWxY4pPaJeoi7QA=w600-h73-v0)

 through our learned function, and produce new outputs. This is the ultimate goal of supervised learning. In supervised learning, as in traditional computer programming, a program still has to be written. However, the purpose of the program, to search for or learn an accurate mapping function instead of pre-specifying it, is fundamentally different. Basic Terminology: Example: Single input-output pairs Features: Input. The part of an example that is fed into the model Labels: Output. The part of an example that is compared with the prediction of the model Dataset: A collection of examples Prediction: The output by a model that has learned from many examples of inputs-output examples and can now take a new input and give a new output Dataset Terminology: Training set: A set of examples that the model is given in order to learn the function that links the inputs to the outputs Validation set: A set of examples that we hold out and do not expose the model to during training, and instead use it periodically to assess, or “validate”, the generalization performance of our model, as we develop the model. We also use it to make meta-level design choices about hyperparameters, aspects of the program that trains the model Test set: A set of examples that we hold out until the very end of the model development process, to double-check the model’s generalization performance on examples that are ‘completely’ unseen during any aspect of model development Training loop: A repeated training procedure that allows the model several chances of learning good, generalizable functions from the training set Training loop structure: 1. Start the program. The program sets up the training environment with a selection of hyperparameters, and initializes the model with a random function 2. Expose the model to examples from the training set, to learn a function from inputs to outputs 3. Evaluate how the function does on the validation set. If the model gets better performance than it ever has before, we save this version of the model 4. Repeat steps 2 and 3 until the performance on the validation set no longer goes up [Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX8PLU4TdR1cOmAVEspt8Bxv2LBELnWi8ucSNzyIj06WM_jbp0cF5SCx7BVBMzvuCxuoy30pxZ85X11v8r3ex3PzEs03RBIhIzzgU2D9xHsNlTJEmZZAoHq7dOd5HDYHoNpOKKcjpg=w442-h129-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX95cOhfJWJt9LjSrCCgv-0gHURJ-XghhURS-ULHBfZ3hOUNha8s283ZYI4_LKEElfuJ21V9Qd_xzrpEbNowc3Z9gdK2Ur7BTEhLaTRm-LXRVpmAKlKGTupGYmwTHwF3oghCkr3lEw=w600-h73-v0)

 Typically, we repeat for various hyperparameter settings. This is known as hyperparameter tuning. Different hyperparameters can produce different models. Once we are satisfied with the model’s performance on the validation set, we can run this final model on the test set. Feature Types: Structured data: A patient’s lab values, diagnosis codes, etc.– also commonly used with the more traditional statistical models. Structured data is commonly input into the model as a list (or vector) of numbers. Unstructured data: Images or natural language (text reports) Images are typically represented as grids of numbers, where each number represents intensity at a given pixel location. In grayscale images, there is only one grid. In color images, there are three grids overlaid on top of each other; the Red, Green, and Blue grids. Texts are typically represented with what are called embeddings. Word embeddings are geometric, numerical vector representations of words. HOW MACHINES LEARN Label types: Labels can be real numerical values. If a model predicts real numerical values, it is solving a regression problem Labels can be categories, or classes. In this case, labels are just numbers that act as category IDs. If a model predicts categories, it is solving a classification problem Model training Mathematically, training minimizes the difference between the output of the model’s function and the true label, for every sample in the training set Loss: The difference between the function output and the true label. Typically, we average or sum the loss for every data point that we have If the model is poorly trained, then will have high loss If it is well trained, then the difference between the true label and our function will be small on average, and thus resulting loss is small as well [Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX-FG9fauXlr-xxz_sR0VpEKA8v9w50Ypjo6bQ9UImzRi1KjqEC7frUgxqA60bC3u0AqhDT0Ggjx3xwragnTIhbZ0jTxNWkvJ5qkRimigDDGmuI6bqMuccMabaV7M1fAvTgepAAN=w442-h129-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX_oOdsQ4n2RuE7JlotJ3cKsNTzi8A_BaezoXq1ivf9Oy8zNBrAT2LvxZHRCZnyXHcE37SYqnP57PwPBbyBDSX5vgKr6Zao52suSC51t05M6ZpK7e_Fq-e3rDGWmI0KIv_mweJ1o=w600-h73-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX9Xqg_RCq0pMe4OOheBifHTGfN1xwbXR_P8VYf5E38firMCXO0pZjQYc-MpQE2u-Do6AvvxmWCtELvlH9CF7piohLeoXICflNSBYq8xcBwHBK2aIt3OA0c9WkEiywp6fnW2UqDH=w858-h352-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX8TCCnjwpv0gS_u0QCjs-60BSCS6nIHg5jldd3X30VULOfPee2Mz_gNNuGQ1oty7F57-rb74VkgwsyXioyWo6mtnt9nM61-REmos5hYuDUePawTM6TUBPr6f4jEz5tpZvz3iOo99Q=w1280-h679-v0)

 The model updates its function to map inputs to labels, as accurately as possible. This is known as fitting or training the model. The model updates its function by adjusting its parameters. Parameters are numerical values that are used to perform operations on the input data. Example: Linear regression. The parameters are the numerical coefficients m and b, as in the equation y=mx + b Parameters that multiply features as weights Parameters that are added to the features as biases It is also common practice to call all parameters– both weights and biases– “weights” Bias (the parameter) vs. Bias (the phenomenon) Bias, the parameter: a number added to features Bias, the phenomenon: a concept that relates to model performance and algorithmic fairness Classification: predicting categorical labels [Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX84wExVKHKGxAR4McERttAQcz_AjVZqb9rbbbfEeNXCi9vo0XdRC2XaZmHs0g-5Sf18fuuQvoNRTT1o-tI8XItMdaVbS9uKT4FRKaD2OeNBHWdnJcgmFZ9eipPnHu6RHrnyvwMcxQ=w442-h129-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX-BQwxhcbbIqh8KfAcDMAohfWk51PZFJriCb-XbvBydjwK1fLkwjri3NIY5x5GSyRFMPisnWJ2-y1kRdndECsq69Z4X5dk50qFn7oLCeLeCSMcD0AUV3NK8gWnR4_V79ZwHV2VLQw=w600-h73-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX_B3L1J-B0jcEcNTIN-MqeoujBAhBDbbjVsREts0zSDHPqOSMsdGZU0xqSwkI5VJbSjKUdRM5pXbe2wTYcAj4OR8WqInmzKaFn_-QLZ8rtBBFT3qORtJtLuusB7EztMtB3yFbuuIw=w1280-h741-v0)

 Difference between classification and regression: Labels are categorical Classification models output probabilities. A model prediction in a classification task is a probability that a given set of features belongs in each category Probabilities are produced using the sigmoid function Logistic regression is a model type commonly used for classification Decision boundary (or operating point) The probability number that we use as our cutoff between categories Commonly the decision boundary is the 50-50 mark Depending on the use case the operating point can be moved Example: screening test in healthcare, perhaps false positives are acceptable and false negatives are not. The operating point can be adjusted so that more examples are classified as positive. The multiple-feature setting is similar to the one-feature setting. Consider the classification setting: In both the one-feature and two-feature cases, we can visualize the label as a separate dimension whose values separate the categories [Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX8mY22HYV_I9LRB_lDI8JcJ2Nco1JLWY7L74wShRClAXPEB2YRusrsXNzouJ0AqpHQGxonoGY2j9haNZUtm9K0FYHb_Ox7z0NoxBHRtOyaMqGOocGMFfKTJdcBHysUMxL8RQtR8fg=w134-h39-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX8GZ4lgc5NzCAN-2__MA4wm7SDBxICHt4HwbuYdOwXVMaPLs0JI-Gou1dYXJc8qLjsr149sBtVrM7mRXxSF6W6YNQkRNpWz83fq8NWfFC6F6zTDmYBwpEun6vochn9rH6U8zpDV7Q=w632-h365-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX-cUIDq6mmoD8E3D1mX6P5_vwr4_aX7WqH7G8YMEDa8ohHavfaR4Xl92bVdrj42-ZikI6AJEEssLZh7E11zoAy87p1_VoXs21pHKYLzZ19kkAmbGKKElDvB6Z241V_Qj0u2CsBd=w442-h129-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX_7QvrC33--ytmWQblnkOqkH2M7vjpO8Z1RU0zbbBQQiIJ8s2Y71xJZTpRn37GS01agFNJFx9L2GBUHJGHnFFdoZ5-Zq3zhRXvIGE9mDxa_tDNcXs-q9jUEDaUv3FeAnCLBUe3F=w600-h73-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX9dlWTyYUKbf-dJPCSxRyKEPtwVg5-di_awTZUQjtuNCJpx9ZBWNNx5uUdSVvkv15l9DQPGdSYBXir8O4J3uyhxMTLYxJbwqavtkdhJacJHXyEhBe-ZlksfnzolMB95rFcjXYAMLA=w719-h529-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX9ElrjSxYb5mc2W0Qg48x46f5Vwjywg0lVaqDl4zJhPHAsgxEyyJ3HwuDQwLojPDecEFQGbKJOGjxyqFkMMa4gAHQVMAbDwVarcoRGtN19OYblhGKOoi_gFJ-n6QcA6ZYunelsNEA=w654-h528-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX8lafKOgDzA387LyUJJ-9LTERsJYmwO7WIIzdO8B6S-nJKNzs8rxgc-7vOauVoGWPu5sUHAF63nUUIlsfgndSB6kP4m-zIISmtpljBlVmDl3rBxX3ZsyKb99kXU9TzumkHqSIU5=w1280-h659-v0)

 The model still has to learn a function with a decision boundary that separates data points with different labels (also called “y’s”) However, in the two-feature case, we need to adjust parameter values corresponding to both features, x1 and x2, to find a good model fit. Recall that the parameter values are the model weights, which are multiplied with the features Drawing the decision boundary is similar as well. Recall that a common decision boundary is where the function outputs 0.5, as in there is a 50-50 chance that the output for a sample sitting on the decision boundary is 1 vs 0. Since y in this case can only take two values, we can flatten this entire figure to two dimensions and mark the y's only through color, in other words a point whose y is 1 is red, and a point whose y is 0 is blue. We can also demarcate the decision boundary by drawing a line everywhere our function equals 0.5. [Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX-sGBoiNlKoayNsLNiEnn9lRPU0X-6HgpHIwLtmkMQVqktr7rV9Iig6WLVHj_YYAzhDAIGTUOAQJ233K-VicZf0MrF1L3GyG6CUNPbykhO37EZVL-EzuiGMaEQr59doJhYGta1I=w134-h39-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX_jEQkT76jyNERQhNl7iPfqnqLkA8GN31gYqzFLGjMEMrWLfoQHM8M0D9J0Le_Yios_gDRyXLc876kpcf4iRDkiri-mNfyd67slVBYxhZS00o8-M4xKBnJfVAkPsBNGUGZcjpomDg=w332-h244-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX9bkJ-vZsDUaIATZ9wyq38zMo1eUKK1LCmnANV4QNqYiuqFskiMCmZeDHN3BfuiFcdkiz0EmM19PToAuo4GDZO9iwE2x-gnfE3lF32_pgZhZWhFk0J9gug74PsKvYT6WxgglL6zqg=w302-h244-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX9dIt55pvvQ_JKjs8dchvhv5qMAf_3MReRQtTF5CBGIIEHLib19LDg22v3k1SvFFYN6h4W8-KpJQAeA-PIOB6vbosMJcABrRBUm0lAqo10Fz1tkJilHS_a84pAgmaVhIiJMJCC-=w613-h316-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX8CIniJ35XBrVEIBl44xcN4WkY36GHJYj2wQuQqWFlsA65Y9NDr9IcETRoGgZwPpvcWvMycPqwVL10u2GrbcLmZtfJcSWxLqTITw3P1EcVpS3x48ktGZEKV6lV8RzSCSqkXHoIiag=w442-h129-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX-GjZNCg6nJITZVTpFXsTydgAgZZElbqIFhNAxL0eFT3P9sP4YsP5otxAQHUQUU80QZJFISbLcsIJ-unK3wg0-cRi4YQCy_n3uxKSJ8DOOFsFLGnq7e3_3ASXE4053-tJSlgHmgaw=w600-h73-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX_Gg7dmDJfbUtyPdBJlpxBe2h7ShWDp23e6oBrfD3O69d4fxwVuOqJYwp_K9xz1nL-yxeQgOY4DRlLP-AjNX3ryQr80bqp3UO1HhfipylZIL20iOdNlxxiSxnD2WWWv_xqf2N8lAQ=w777-h638-v0)

 The two-feature setting is a straightforward extension of the 1-feature setting. While it is more difficult to visualize, the same idea holds for any number of features. For binary classification, the geometric intuition is the same: find a function whose decision boundary sits between the two sets of samples. SUPERVISED MACHINE LEARNING The “No Free Lunch” theorem: No one Machine Learning algorithm is best for all problems Regression variant: Polynomial Regression Useful for handling non-linearly separable data where the best fit line is not a straight line It fits a curve instead of a line Other common regression variants Examples: Lasso Regression, Ridge Regression, ElasticNet Regression At their core, they are all functions that can act as a classifier and can be adjusted to better fit the relationships between the features to predict the correct label. TRADITIONAL MACHINE LEARNING Decision tree algorithms, specifically classification tree algorithms, generate a tree structure where the branching points are decision points that are based on the relationships between features found in the training dataset. [Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX-AAqCDeNCRcOBUS4W0pJs2bnSO1Jnrq7qPxJz-Hmw2zTCxIqFbufFczHKxLngOFSdDR4GiFX8rVTPy22v3nij2ragHyC0EgMJOaT3mV2234vIMAMt1ffyrwTvikHiawKp4bQVB2w=w442-h129-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX-OxXH-3rJ9BF722658bPgIcGnHoqJNmOb8FlOOJua-HVRtDU_zuGJcEg4lX-IwBd_QbyDLAGKY6xOeOmI1DhXhSKOpjtHVIYOacFUBRqYhlbVDjx3-mKfI605RdP-8iLgedUQz=w600-h73-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX-4p6KwUrRKKRAvytIvmlZG2VQTxmmrwmH75GxJMjUzuVF8APYYcDgRh-zCyOoLdeKt7r-kRBTug9tfEkUDT1Zq_aGk0G_Q2lkSSA14Bf-FCflgWJ4nKiFbU2M7xXAnnGEbeFos=w1089-h704-v0)

 Advantages: They can be very fast to train with high dimensional datasets They are simple to understand and interpret– every branch in the tree is a decision point based on some relationship between the features Disadvantages: They can sometimes be inaccurate. This effect can be mitigated by using decision tree variants such as Random Forests Random forests improve decision trees by ensembling, or combining, the predictions of many, many decision trees. Typically, decision trees are trained on all features using all samples in the training dataset. Each of the decision trees in a random forest algorithm only (1) sees a subset of the features made available for each sample and (2) sees a subset of the samples in the training dataset. Advantages: The diversification of decision trees may produce some bad classifiers, but many other trees will be right. So, as a group, the trees are able to classify things more correctly than any single decision tree. Disadvantages: Slower to train than decision trees. Support Vector Machine (SVM) is another supervised learning machine learning algorithm used for classification problems similar to Logistic Regression (LR). Logistic Regression considers all samples equally[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX8Ntc_y70Smh7AxLW3hULr82lA11alxOoj_E9iNF59BmBDNh2vZ3aE6fAImNN87e-qTaVIQzWQ-V5fHmWzT0zeGSwtvb768h3KIL0Mn7AWmrddZvaY530a3pT-mj7MDRPrXwOSCnA=w442-h129-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX-ff6SsxyF_C2BA_9l3k3j14IpVJ1OzR2QenpyWIvwPHE39l3bOyB3GpeWu4CWwdnNXma0Py6DjWR3LdDidLcKm9SwplKRkNN3WUC_F2Zo54aH0DP_jytClPxFcXmpPMoFVdwOcfg=w600-h73-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX9S95m-n1hGM4WJSZxk8B1MdoXNDvrxDAI6HJDxTYFQ-HaZQ5CUSYoFjp9gc05VUwYznm3IKQKj0z46jDFSICa5ze8R6wgm1QAmDcEXL4GPkjyUSe3hcoT6VSsX6AFtqqjyLyba7w=w1280-h476-v0)

 SVMs consider samples that are near the decision boundary more strongly than Logistic Regression, which in turn makes SVMs more robust to outliers A large dataset with no labels at all and no feasible way to label them on your own Unsupervised learning seeks to examine a collection of unlabeled examples and group them by some notion of shared commonality. Clustering is one of the common unsupervised learning tasks. We can use clustering to define the label or category In unsupervised learning the difficulty lies not in obtaining the grouping, but in evaluating it or determining whether the grouping that is found is actually meaningful. The challenge, then, is whether the presence of the groups (i.e., clusters) or learning that a new patient is deemed a member of a certain group is actionable in the form of offering different treatment options or making a clinical decision. CITATIONS AND ADDITIONAL READINGS **Hastie, T., Tibshirani, R., & Friedman, J. H. (2001). The elements of statistical learning: data mining, inference, and prediction. New York: Springer. **Matheny, M., Israni, S. T., Ahmed, M., & Whicher, D. (2020). Artificial intelligence in health care: **The hope, the hype, the promise, the peril. Natl Acad Med, 94-97. https://nam.edu/artificial-**intelligence-special-publication/ Shah, S. J., Katz, D. H., Selvaraj, S., Burke, M. A., Yancy, C. W., Gheorghiade, M., Bonow, R. O., Huang, C. C., & Deo, R. C. (2015). Phenomapping for novel classification of heart failure with [Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX8BiOB7aZ269H2tlUOLv1YCfzxdKJaYz3Cay58axl06pgkROwBZYfzjgCybSRNH7LVeHi4Ap-rlPBrUINK3haQ8U-zluXbJNcfNI6rtD_uydzmJ5EzJ5Yeqn-VJBhXBAX8u_qlS=w442-h129-v0)

[Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX_dSjPeXbOxWZdd2und_-8eG-bmQhFQ8UKdDXo5F47ueYJAqV__L1x3t_UrpWUqrSqQSKcOwktJHNu9ILUqcfiHkuHQhSHZCzGRwivDoD01bweXPfkbeNAshIKO7CYYPmqz7xAmmw=w600-h73-v0)

 **preserved ejection fraction. Circulation, 131(3), 269–279. **https://www.ncbi.nlm.nih.gov/pubmed/25398313 Shah, S. J., Katz, D. H., Selvaraj, S., Burke, M. A., Yancy, C. W., Gheorghiade, M., Bonow, R. O., Huang, C. & Deo, R. C. (2015). Phenomapping for novel classification of heart failure with **preserved ejection fraction. Circulation, 131(3), 269-279. **https://www.ahajournals.org/doi/10.1161/circulationaha.114.010637 [Image](https://lh3.googleusercontent.com/notebooklm/AKYWMX9tJgInzt8FJ1unKDAf-vg75ncHdC2TjEywRSpCqTGILMWnEyKoBQjpAAcbvYtGSnKWn7-iHrZRdYpfm6nWFprKaPh8e3udxglWjGeFVhjHU3x19OB2OpmvYwxl-koLkpcHU7_XfA=w134-h39-v0)

---

## 2. How Machines Learn Part 1.mp4

### Summary

The provided text explains the fundamental mechanics of supervised learning by distinguishing between **regression** and **classification** problems. In regression, models predict **continuous numerical values**, such as BMI, by minimizing the **loss**—the discrepancy between predicted and actual labels—through the adjustment of internal **weights and biases**. Conversely, classification tasks categorize data into distinct groups, often employing a **sigmoid operation** to transform linear outputs into **interpretable probabilities** ranging from zero to one. This process, exemplified by **logistic regression**, requires the selection of a specific **threshold** or decision boundary to determine final categorical assignments. Ultimately, the source illustrates how machines autonomously **learn mathematical functions** from labeled examples to solve complex predictive challenges across structured and unstructured data.

**Keywords:** Supervised learning, Regression versus classification, Model training process, Loss and parameters, Logistic regression thresholds

### Content

Okay, so now that we have some working definitions for inputs and outputs, features and labels, we can dive into some concrete examples and put them into context. In supervised learning, these output labels can take many different forms. They can represent real numerical values or they can represent categories. When the labels represent real numerical values, we say that the model is solving a regression problem. If the model labels represent categories, we say that the model is solving a classification problem since it is trying to classify examples into their respective categories. All right. So, let's say that we want to predict patient BMI given their weight. BMI or body mass index is a numerical value and weight is a number with particular significance. Since the label BMI is a numerical value, we can formulate this as a regression problem. Specifically, one might frame this problem as a regression problem that uses structured data. To take a machine learning approach, if we have a data set of patient weights with their corresponding known BMIs or labels, we could then split our data set into training, validation, and test set. Now, even though this equation can easily be programmed by hand, And remember we talked about that before using our known rules to write a function that can then take input and transform it into an output. But here we're setting this up so that the model can use supervised learning to learn that function from the input and output examples. We're not going to tell it the BMI equation. The key here is that we can do this without knowing what the equation or function is to calculate BMI in the first place. Now we're expecting the model to learn that function through many examples. It sounds like an inefficient way to do this for this simple example, but it gets really useful for more complex problems. So for this example, we would then train or fit a model, in this case, let's say a linear regression model on this input feature set. Model training involves mathematically speaking, minimizing the difference between the output of the model's function and the true label for every sample in the training set. The difference between the function output and the true label is known as the loss. Typically, we average or sum the loss for every data point that we have. If our model is poorly trained, then we will have a high loss. If it is well trained, then the difference between the true label and our function will be small on average and thus a resulting loss is also small. In order to minimize loss, the model updates its function to map as accurately as possible from inputs to known outputs or labels. This is known as fitting or training the model. The model updates its function by adjusting its parameters. The parameters are numerical values that are used to perform operations on the input data or simply speaking the values that are typically multiplied with or added to the input features or when they exist the intermed. outputs of the function. In our example of linear regression, the parameters are the numerical coefficients m and b as in the equation that we all know well, y= mx plus b. Typically, we refer to parameters that multiply features as weights and parameters that are added to features as biases. Now, at this point, I'd like to note just a couple things. First, in machine learning, there are actually two highly prominent definitions of bias. One of the uses of the word has to do with concepts around the model performance and algorithmic fairness. The other is just a descriptor for additive parameters. Our use of the word bias is now the latter, but we will talk more about the former in a later section. And as a second note, it is also very common practice to call all parameters, both weights and biases, weights. So now we've described how we can split our data set into training, validation, and test sets, and elaborated a little bit on the notion of loss and the process of fitting a model to that training data. Once we fit our model, we would then validate the model's performance on the validation set. In our simple example here, we don't have any hyperparameters, so we're good to go. Finally, we would run our model one time on the test set and then report its final performance.

Now, let's talk about a different example. What if we wanted to predict whether a nodule in the lung on a CT scan is malignant or benign based on the size? Remember that classification is similar to regression and that we want to train a model to accurately map input features to their corresponding labels. However, in classification, We have labels that are categorical. The features could be unstructured data like pixels of an image and the labels might be the categories of what's present in the image. For example, lung nodule, skin cancer, knee arthritis, etc. Or your features could be structured data like lab values, diagnosis codes, age. And the labels might be patient outcomes like heart attack, sepsis, or mortality. Your features could also be unstructured text in nursing notes or pathology reports. And the categorical labels could be the final diagnosis for that. counter like stroke or appendicitis. In our simple example of predicting malignant or benign based on lung nodule size, let's say that each example in our data set consists of one input feature, the size of a given nodule. The feature would be given a categorical label that is binary. It's either zero if the nodule is benign or one if the nodule is malignant. So, if we were to plot our data, we might see the following. Note that these data points take a very different shape than they did in our regression model. If we were fit a line to the data, we'd end up with the following. There are a couple of things wrong with this. First off, this model would have a lot of difficulty if our training set contained any outliers. Imagine how the line would change if we had an extreme point. We would also like to be able to interpret the output of our model. In our regression model example that we fit for BMI prediction, we could produce an easily interpretable output that was simply the value of their predicted BMI. But in this case of benign or malignant, it's not clear what a function output of say 1.7 means when we care about whether a nodule is a binary categorization of either benign or malignant. Something that's more natural and useful would be a probability. Concretely, we would like it if our function could actually output a probability of whether a given nodule is benign or malignant. And probabilities are between 0 and one and they'd have an interpretable meaning in this context. So to achieve a probability output, instead of fitting a simple line, we could actually fit a function which has a sigmoid operation at the end. And the role of the sigmoid would be to squash all the functions values and put them between 0 and one. The sigmoid operation is a nonlinear transformation where in simple terms if a line is passed in the output is a more complex shape and in this case something that looks like an S. We'll cover more nonlinear transformations later on in this course. But for now consider what the sigmoid does for us. It'll allow our overall mapping from input to output to look something like this. Now if we take a look back at the problems that we'd mentioned using those void allows us first of all to be robust to outliers. If a nodule is very big, the model will likely just predict a value very close to one. Second, it also allows us to have a more interpretable output since the function outputting values between 0 and one can now be interpreted as producing probabilities. What we've just described forms the backbone of what is known as logistic regression, a model type that's commonly used for classification. Now that we have a shape for a function that fits naturally into our problem type, we can train the model. model. Remember that model training is simply minimizing the loss on the training set.

So before we move on, there's one more thing worth pointing out about classification. Take the hypothetical situation where we have a perfectly fit function. In that case, the model would be 100% confident about the classification. But we all know that in medicine, like the rest of life, there is not black and white. Instead, there's a whole lot of gray area that exists between the high and the low probabilities. So somewhere in the range of probabilities for a model output, there has to be a dividing line to define the outputs. as the function will likely not be perfectly fit to the data. In a medical use case, there has to be a probability that divides those predicted to have a disease or condition and those that do not. If we're talking about our lung cancer example from earlier in the range of probabilities from 0 to one, we have to find the cutoff in that range to decide yes for a malignancy. Does that make sense? The probability cutoff is referred to as a threshold, also known as the decision boundary or the operating point. The deciding point is commonly sort of by default place at the 50/50 mark. However, we can and should change that depending on the kind of performance we actually want and need from our model. So, if we want our model to be very sensitive, like for a screening test in healthcare, for example, we might want to say that everything above 30% probability should be considered positive for that disease or outcome. But doing this may have unintended consequences around an unfavorable number of false positive cases. And there are other considerations which will lead into our broader discussion on metrics and the decisions around choices that impact the utility of our model. These sorts of decisions are those that drive a lot of the analysis done during model evaluation. And it matters a lot the kinds of metrics that one uses to measure the model performance as well as the pre-est probability and other epidemiologic factors. All of which we will cover more in detail later on.

---

## 3. How Machines Learn Part 2.mp4

### Summary

This text explains how supervised machine learning evolves from simple models to complex systems by analyzing the **multiple feature setting** in binary classification. By assigning **parameters or weights** to various data points, a model learns to establish a **decision boundary** that effectively separates different categories of information. While basic problems can be visualized geometrically, more sophisticated tasks like image recognition require **layering parameters** to create complex features, a fundamental concept behind **neural networks and deep learning**. Ultimately, the goal of this training process is to **minimize error** so the model can provide accurate predictions when presented with new, unlabeled data.

**Keywords:** Binary classification, Decision boundary, Supervised machine learning, Model training, Neural networks

### Content

We've already defined linear regression and binary classification in a one feature setting. Next, we'll talk a little bit about the multiple feature setting and discuss in more detail the specifics of model training. The one feature setting for linear regression and binary classification seems simple, but it illustrates many concepts that hold true regardless of the number of features that there might be. Consider the two feature setting for binary classification. Let's say that we want to make a prediction as to whether or not a given patient has pneumonia by taking taking a look at two features. One, the white blood cell count, and two, the patient's temperature. Let's also call these features X1 and X2. This is a binary classification problem. For a particular example, the label, which let's call Y, is zero if the patient is normal or one if the patient is abnormal. And the features are the patient's white blood cell count and temperature. We were able to plot our earlier single feature example on a two-dimensional coordinate plane. Since we only have one an extra feature. Now we can still plot this but this time in three dimensions. And so indeed if it helps you can think of samples geometrically as points or coordinates. Here we see that in three dimensions the patients with pneumonia sit on a different plane than those without pneumonia. The model still has to learn a function with a decision boundary that separates data points with different y's. But now if we follow our same model structure as in the earlier examples, we have parameters or weights that are multiplying both features x1 and and x2. So we'll have to adjust the parameter values corresponding to both of these features in order to find a good model fit. Remember that learning our model function is a matter of minimizing the loss or error between the functions output values and the labels values for a particular set of features. In this example, this could look like the following. Here we can visualize a decision boundary once more. Remember that a common decision boundary is where the function outputs 0.5 as in there's a 50/50 chance that the output for a sample sitting on the decision boundary is 1 versus zero. In our example, we want the function to have values close to one for every sample that is on one side of the decision boundary and vice versa for the other side. Since y in this case can only take two values, we can flatten this entire figure to two dimensions and mark the y's only through color. In other words, a point whose y is one is red and a point whose y is zero is blue. We can also demarcate the decision boundary by drawing a line everywhere. or function equals 0.5. We often use this type of illustration to make things easier to visualize. You'll probably see this in other settings as you continue your experience in machine learning as well. And that's it. The two feature setting is a straightforward extension of the one feature setting. While it's more difficult to visualize, the same idea holds for any number of features. For binary classification, the geometric intuition is the same. Find a function whose decision boundary sits between the two sets of samples. With structured data, it's often possible to find a good function by adjusting parameters directly multiplying each of the features. However, when we're dealing with feature vectors that are of a much higher dimension, such as with unstructured data, it's often necessary to layer their parameters. This means creating combinations of the input features in a way that becomes more complex features. In the case of images, that might mean grouping the pixels of an image into features representing the presence of shapes or lines. In the case of natural language, That might mean combining the embedding of a word with embedding of another word in order to create contextaware embeddings. Layered features and parameters is a key part of the intuition behind neural networks and deep learning. Something that we'll cover a bit later in this course. Before we can do that though, let's take a look at some of the more traditional algorithms used in machine learning.

Okay, so we've covered a lot. Let's recap what we've learned so far about supervised machine learning. First, we covered so many terms and many of them seem to have more than one term to describe the same thing like out put label and prediction. That's okay, though. As long as you can start to understand how they're used conceptually, you're doing great. We've also discussed that machine learning, which is supervised, refers to obtaining a data set with a labeled input output pair or example where the input contains any number of features and we know what the desired model output or label is. And then we can fit a model that can accurately discriminate the labels based on the relative importance of the features. Examples of problems that are great for supervised learning include class ification and regression as we've covered. And as we'll see, the types of algorithms that can do these tasks well range from the very simple like regression to the very complex and slightly more exciting the deep neural networks. Now, when you read about a new machine learning model that was trained to recognize if a photograph of a skin lesion is cancer or not, you will already understand some of the key components of that work. And we haven't even covered that algorithm yet. We told you that supervised machine learning was used based on a data set of labeled images. This label data set was used to train the model. The input features are each pixel in the image, hundreds of thousands of features in this case. And the output is a binary categorization of cancer versus no cancer. And there could be more categories. After we've trained the model, in other words, learned values for the parameters or weights and obtain the decision boundary, we should be able to input new unlabeled images into the model and get good predictions for cancer versus no cancer. And it should work. But the principle behind the supervised machine learning approach was the same as it would have been for any other type of model or data set. All right, hopefully you're feeling pretty good so far about what we've covered. Later on, we're going to talk more about the different algorithm types and how the model learns the parameters or weights for each feature and then how different metrics for performance are also considered. But next, we're going to go on and talk about the spectrum of different machine learning models.

---

## 4. Machine Learning Terms, Definitions, and Jargon Part 1.mp4

### Summary

The text explores the fundamental transition from **traditional rules-based programming** to the modern paradigm of **machine learning** within the context of healthcare diagnostics. In conventional systems, human experts must manually code every specific decision and mathematical rule into a **function** to process data, a method that often proves too rigid for complex tasks like interpreting an ECG. In contrast, **supervised learning** allows a computer to act as a **function approximator**, automatically discovering the relationship between data inputs and known output labels by analyzing thousands of examples. By focusing on this **spectrum of supervision**, the source illustrates how models can eventually match human-level performance in predicting mortality or diagnosing diseases without requiring **explicit programming** for every possible clinical scenario.

**Keywords:** Machine learning jargon, Supervised learning, Traditional computer programming, Medical data labels, Mathematical modeling techniques

### Content

We've already started to discuss some of the basic concepts related to machine learning and how it was different from traditional computer programming earlier. Now, we're going to delve more deeply into that definition and introduce several techniques as well as relevant data types and use cases that will hopefully lead to a working knowledge for you in subsequent discussions. In other words, it's now time to talk about the jargon of machine learning. It'll feel a bit like learning a new language, but even after only a short time relating to the terms concepts and principles, it'll hopefully be possible for you to confidently move forward with pursuing your machine learning and healthcare journey on your own. This will hopefully mean that you'll be well prepared to identify areas of interest and seek out more resources on your own that'll deepen your knowledge of these concepts and principles. Machine learning is endlessly fascinating and constantly evolving, just like healthcare. And both are lifelong learning endeavors for everyone. Don't worry if this seems like a lot coming at you fast. especially if you're new to this. We'll make sure that you hear these terms and principles many times and from lots of different angles throughout the next several conversations. Bottom line is don't panic. As we discussed earlier, machine learning is a family of statistical and mathematical modeling techniques that uses a variety of approaches to automatically learn and improve the prediction of a target objective without explicit programming. An even more concise way of thinking about this is systems that improve their performance in a given task through exposure to experience or data. Often people talk about machine learning as having three paradigms, supervised, unsupervised, and reinforcement learning. Each of these approaches can address different needs within healthcare. We'll define these terms soon, but the important thing to know for now is that it's more accurate to describe machine learning problems as falling along a spectrum of supervision between these terms. For the sake of simplicity, we'll focus primarily on unsupervised learning. But it's good to recognize that other paradigms also exist and will in some ways fall on the spectrum.

A useful way to think about what we're going to use in computer science here boils down to three components. There's an input, some kind of processing, and an output. Using a simple example, we can think of an equation. Told you about the equations earlier. This one's easy. Y= X^2. In this case, the input is X and there's an output Y. Or another example might be abnormality detection where the input is an ECG and the output is a medical diagnosis like ST elevation myioardial infarction. For both these examples in between the input and the output there is something that processes the input to produce the output. So in the first example that simple equation it is squaring the input X to arrive at the output Y. In the ECG example there is a visual analysis being performed on the ECG leading to that output diagnosis. In both cases that processing we are referring to is the thing that transforms the input into the output and that's called a function. In a traditional computer programming approach, we deliberately write rules to process the inputs so that they produce the desired outputs by hand. This is why traditional computer programming is also referred to as a rules-based approach. In other words, computer programmers write functions with specific rules and the program that is written is the function or is the thing doing the processing to achieve an output. So if we were to write program. For the first example above, the program would ask for an input X, then square the input, and return the new output Y. The function here is the part that squares the input. The same thing would need to happen if we wanted to take a traditional computer programming approach to the ECG example, right? But obviously, the function here will be well much more complicated than just simply squaring the input. But you can imagine that even without programming experience that you would need to somehow write a program that would process the ECG in a series of very specific steps and decisions. so that an accurate analysis of the ECG can be performed. That resulting program would be the function that would process an ECG and provide an output. In this case, maybe heart attack or no heart attack. And this, by the way, is exactly how the early automatic ECG reading machines that were used in hospitals all over the world are able to provide a prelim diagnostic analysis. They relied on this traditional computer programming approach like the one we're describing, which required that the function be coded by this laborious rules-based approach. In this case, Due to the complexity, it required lots of coding by programming teams as well as domain experts to help decide from the medical point of view and the computer programming view how to interpret the diagnostic decisions and put that into a function.

In contrast, in a machine learning and in particular a supervised learning approach, we don't know exactly how to process inputs to produce the right outputs, but we have a lot of data that links inputs to the known corresponding outputs. The program written in this type of approach now search is for or in other words learns or finds a function that can accurately map our data of inputs to the outputs. We can then use this function to process new inputs and produce new and hopefully correct outputs. So in our earlier example of ECG diagnosis, we explained how difficult it would be to write a function that would process ECG input data to accurately give a diagnosis as an output. The inputs in this case are complex and vary from patient to patient and for the most part the Traditional ECG automated diagnosis systems aren't terribly reliable, or at least they're not used to making final decisions on their own. This is because for the most part, humans haven't been able to handwrite a set of rules that can give us human level performance on these incredibly complex tasks. No matter how sophisticated the function we can write is, in the end, they're still too simplistic and typically too rigid to succeed broadly in the real world. After all, if it were possible to do this efficiently and accurately, there wouldn't be any need for us to be teaching this course. So in contrast, let's think about how the machine learning approach would work here and why it might be an advantage. While the function from input to output can be difficult to write down, very often we do know what the right answer or the output is supposed to be. If a cardiologist has already looked at the ECG, the input, and recorded a diagnosis or the output, then we likely have two parts to the equation. All we need to do is figure out the function that solves this input. output relationship. Supervised learning is a process through which a program takes input output pairs and learns the function that links them together. Note that we call this supervised learning because we provide input and output pairs. We are supervising the model by providing it with the right answers. The entity that underos supervised learning is commonly called a model because it represents or models the relationship between the inputs and the outputs. Learning this relationship means learning a function which which in this case means adjusting a set of numbers known as parameters. A model is defined entirely by its parameters and the operations between them. The field of machine learning is largely dedicated to discovering algorithms and techniques for finding good values of parameters that allow models to match inputs closely with their corresponding outputs. Sometimes you might also hear someone call a model a function approximator. And this is because this is exactly what it does. It approximates the function between the inputs and the outputs. Once the program learns a function that works well, we can then use it in place of software that would have been written by traditional computer programming. We can take new inputs, put them through our learned function, and produce new outputs. And this is the ultimate goal of supervised learning. Now, I want to pause here and clarify one important point. In supervised learning, as in traditional computer programming, a program still has to be written. However, as we've discussed, the purpose of the program to search for learn an accurate mapping function instead of prespecifying it is fundamentally different. We'll spend some time now discussing what must go into supervised learning programs in order to create a working machine learning model. In our ECG example, we would expect that with enough inputs or ECGs and outputs or diagnosis, the function can be learned by a model and we wouldn't have to explicitly program each step of the function by hand. The model would learn to read ECGs like a cardiologist. Pretty cool, right? And so because supervised learning relies on labels, the success of any supervised machine learning approach is dependent on the labels being correct. The labels are the output. They are the result of the function acting on the input. Make sense? So we call it supervised learning again because we provide the input and correct output labels to the machine learning model as a supervisor who teaches the learning algorithm. The machine learning model learns in a supervised way through many examples of inputs and output labels how to eventually build a function that can accurately take new data that doesn't have any labels and reproduce labels accurately as output. Again, the labels in this case can be anything. A medical condition like diabetes or Parkinson's disease, an event like readmission to the hospital or sepsis, an imaging diagnosis like skin cancer from photographs of lesions or pneumonia on chest X-rays, and like our example earlier, it can be a heart attack on an ECG. Basically, anything that represents a desired output can be a label. The labels represent what our machine learning model should output or tell us given their respective inputs. If we teach the model with examples of what it should do given a particular input. The idea is that the function will be learned. Since a lot of data is needed to do this very well with these models, we have benefited from this approach now that we have the rapidly expanding availability of medical data and there have been many successful applications of supervised learning to healthcare. This method of training a model is popular in settings with a clear outcome and in particular a setting that has large amounts of label data. As an example, there was a group from Google and UCSF who worked together to create a machine learning project with supervised learning to predict mortality, readmission, and diagnosis labels at discharge from EHR record data. The resulting model was able to then take new data and subsequently predict the correct labels from that data. In another example, Stanford researchers developed an AI system that classified 14 conditions on a chest X-ray at a performance level compared to that of practicing radiologists. Based on tens of thousands of input output example pairs, their system learned which image features were most closely associated with the differential diagnosis just like a human. So over time and hundreds of thousands of examples of images which was the input and the diagnosis which was the output label, the AI model was able to learn an accurate function that performed as well as human experts for reading chest X-rays. Again, these sorts of examples did not require any of the laborious rules-based programming that we would see in a traditional approach, which would take decades to achieve the same kind of performance. Pretty cool, right?

---

## 5. Machine Learning Terms, Definitions, and Jargon Part 2.mp4

### Summary

In supervised machine learning, developers utilize a structured **data set** composed of **features** (inputs) and **labels** (outputs) to teach a model how to make accurate **predictions** on information it has never seen. This educational process is organized into a **training loop**, where the model learns from a **training set** while its ability to **generalize** is periodically measured using a separate **validation set**. To ensure the final system truly works in the real world without "cheating," a **test set** is held out until the very end to provide an unbiased evaluation of performance. This workflow accommodates both **structured data**, like numerical lab values, and **unstructured data**, which is converted into a mathematical format through pixel grids for images or **word embeddings** for text. Ultimately, the goal is to fine-tune **hyperparameters** so the model discovers the most effective function for linking complex inputs to meaningful, reliable outcomes.

**Keywords:** Supervised learning, Data set splitting, Model generalization, Hyperparameter tuning, Features and labels

### Content

Now that we have a working definition for supervised learning, let's take a look at what this process actually looks like. We'll start with a bit of terminology. From this point on, we'll refer to inputs as features and outputs as labels. We'll refer to a single input output pair as an example, as in an example to show our model. And then we'll refer to a collection of examples as a data set. We'll be using these terms interchangeably. This isn't to confuse you, but to get you comfortable with the fact that they are used interchangeably in everyday practice and it can be much more confusing later on if you aren't comfortable with these terms and concepts from a few different angles. Sound good? Okay, let's cover a few more terms then. So, commonly a data set is split into what's called a training set, a validation set, and a test set. The training set is a set of examples or input output pairs that the model is given in order to learn the function that links the inputs to the outputs. The difference between the many types of machine learning models that exist today is the process through which the right function is found. Now, what makes a function the right function? Well, a common misconception is that a function is good if it can accurately link inputs to outputs on the training set. However, we need to remember our real goal. We want to find a model that can process new inputs and produce new and accurate outputs. This means that we can't evaluate the model on inputs it's already seen. We want to ensure that our model can generalize what it's learned to new inputs from real world applications, which means evaluating it on examples that it was not already exposed to during function learning, which would essentially be cheating. In other words, we want to evaluate our model on unseen examples. If a model can produce accurate outputs for these unseen examples, then we can say that the model generalizes well to new inputs. This brings us to the validation set. The validation set is a set of examples or input output pairs. that we hold out and do not expose the model to during training and instead we use it to periodically assess or validate the generalization performance of our model as we develop the model. We use the validation set in order to get a sense of when the model has found a function that generalizes well. In practice, we can run the model on the validation set intermittently during training. And remember, we can turn off the learning mode of the program while doing so so that the model doesn't actually update its function using these particular examples. And since this is a program, we can design it to do whatever we want. And then we can save the version of the model that gets the best validation set performance. We can also compare performance on the validation set to make metal-level design choices regarding aspects of the program that trains the model and are the function types that we consider during training. We call these other aspects hyperparameters. Now, there's one final thing that we need to consider, which is that even though we do not let our model learn using the validation set examples, We do still compare performance on the validation set to tweak and tune the meta- level hyperparameters that we mentioned of the learning program. In the process, we may be inadvertently helping the model to do better on these particular examples. So to address this, we have one final set of data that we call the test set. The test set is a set of examples that we hold out until the very end of the model development process to doublech checkck the model generalization performance on examples that are completely unseen. During any aspect of model development, we can evaluate our final learned model on the test set to get the most accurate assessment of expected generalization performance. To summarize, we've defined one, the training set, which we expose the model to in order to learn function parameters. Two, the validation set, which we do not allow the model to learn from, but which we use periodically to estimate generalization performance and to choose metal level hyperparameters of the learning program. And then finally three, the test set. which we hold out until the very end of model development and run our final model on once to get an accurate test of expected model performance on completely unseen data. Now, with these three data sets in mind, let's see how they're used in practice. Typically, model training takes the form of a training loop. The training loop refers to a repeated training procedure that allows a model several chances of learning good generalizable functions from the training set. The training loop is pretty simple. We start with a program. The program sets up the training environment with a selection of hyperparameters and initializes the model with a random function. At this point, we set the model's best performance to be zero. We can then expose the model to examples from the training set in order to learn a function from inputs to outputs. We evaluate how the function does on the validation set. If the model gets better performance than it ever has before, we save this version of the model. And so we can repeat the second and third steps of training the model and assessing in the validation performance until the performance on the validation set no longer goes up. Typically, we'll repeat the above for various hyperparameter settings. This is known as hyperparameter tuning. And different hyperparameters can produce different models. Now, once we're satisfied with the model's performance on the validation set, we can run this final model once on the test set in order to doubly confirm the model's performance on completely unseen examples. This training loop is at the heart of the machine learning process. A lot of the machine learning principles that will continue continue to talk about in this course. We'll focus on one different kinds of machine learning models that are common for this training procedure and two highle design decisions that we can make to produce high performing models. Let's pause here for an important note. Don't worry at all if some of this seems unfamiliar and even a bit intimidating. Depending on your prior background, we will be covering these concepts many many times in the coming lessons with plenty of medical examples. Uh so our not so secret plan now that I'm telling you it's really not secret anymore is that by introducing many terms and concepts that upfront over subsequent lessons with more rich examples and longer discussions. All of this will start to come together for you and become more and more familiar and hopefully ideally even intuitive. It can definitely seem like a lot depending on your background, but we are confident that we've done this in a way that by the end you'll feel like a total pro. And yes, you'll have to trust us on this, but since we've spent all this time together, we aren't strangers anymore, right? So, let's move on. We've talked before about examples in a data set and how data sets are split up for the sake of training. machine learning models. But what do these examples really look like? Recall that an example is simply an input output pair. Alternatively, we also said that the input could be described as a set of features and the output was the label. When the model has learned from many examples or input output pairs, it can now take a new input and give a new output. That output by the model is typically called the prediction. And as we covered earlier, features are the pieces of information given to the model in order to make a prediction. It's the data or the stuff that we give the model so that it can give us information we want as output back to us. Again, the input features could be structured data, for example, a patient's lab values or diagnosis codes, etc. These sorts of inputs, structured data, are also commonly used with the more traditional statistical models you may be familiar with. Structured data is commonly input into the model as a list or a vector of numbers. The input features can also be unstructured data. Unstructured data are things like images or natural language and text reports. For example. While it may be intuitive of how structured data can be mathematically used by a model or program, after all, they're numbers. How can unstructured data or features be mathematically used by a model? That's a great question. I'm glad you asked. In the case of medical images, they're typically represented as grids of numbers where each number represents the intensity at a given pixel location. In grayscale images, there is only one grid. In color images, there are actually three grids of numbers overlaid on top of each other. The red, the green, and the blue grids. Each of the is representing the intensity of its respective color at those pixel locations. In the case of free text, blocks of text are typically represented with what are called word embeddings. Word embeddings are geometric numerical vector representations of words. We can think about this intuitively in a two-dimensional geometric space. If you were to imagine placing words as objects in front of you on a flat surface, you might see that certain words are related and clustered together. In medical text, they might be cough and fever, while a word like cholesterol would be somewhat further away on in this little grid and that might be associated with triglycerides for example. The corresponding numerical coordinates of each word, in other words, its vector representation is what is known as its embedding and this is what helps mathematically represent and relate these words together with one another with numbers similar to the way that pixels must be represented as numbers. Of course, we can also use word embeddings in higher dimensional spaces than two-dimensional, but the two-dimensional case is easiest to understand from an intuitive perspective. Word embedding representations allow a model to interpret words as numbers and therefore as features so they can be used as input for a model. And so returning to the concept of labels, which we will cover more throughout this course, the label is again the output or prediction and could be a diagnosis or an outcome or really anything you want the model to give you back with a given type of input or features. The features or input could be a structured data like I've mentioned like lab values or unstructured data like X-ray images or free text reports. With many examples of the input features and output label, a model can eventually learn a function to reliably produce the label when given new unlabeled input features. Set it in another way, when a model is given a set of features, a trained model should ideally produce an output that matches the features corresponding label. All right, so that's a lot and I want you to feel free to review these first few videos a couple times to get the hang of all this new vocabulary if it still seems unfamiliar to you. But again, don't worry, we're going to cover a lot more of this next.

---

## 6. Other Traditional Supervised Machine Learning Approaches.mp4

### Summary

This text explores the mechanics of **decision tree algorithms**, which function by creating **logical branching structures** to categorize data and make predictions. While individual trees are prized for their **transparency and interpretability**, they often suffer from limited accuracy and a high sensitivity to small data fluctuations. To overcome these weaknesses, the **random forest** approach utilizes an **ensemble of diverse trees** to produce a more robust and reliable outcome. Much like a **diversified investment portfolio**, this method relies on the **"wisdom of the crowd"** to ensure that the collective prediction is significantly more precise than any single model acting alone.

**Keywords:** Decision tree algorithms, Classification accuracy, Random forest models, Feature importance, Ensemble learning methods

### Content

Another class of machine learning algorithms are decision tree algorithms and these construct a model based on decisions that are related to the features in the data set. The decisions fork into tree structures until a prediction is made for any given data point. These algorithms are very fast to train and are favorite in machine learning. Examples of some types of these models include random forest and conditional decision trees. What if we wanted to apply decision trees to our pneumonia data set from earlier where we wanted to predict whether or not a patient with a cough has pneumonia from two clinical variables, temperature and white blood cell count. The decision tree algorithm would attempt to learn or fit logical branching points. And based on our example data, this is likely how the overall structure would look with the algorithm evaluating each decision node for how well it would contribute to discriminating pneumonia versus no pneumonia in our data and choosing the decision tree that best fits the data compared to other less ideal variants. Although a real data set would have a lot more features and this would just be a branch in a much bigger tree, the simplicity of this algorithm is very attractive because the features that are of most importance to the label are very clear and relations can be viewed easily. This methodology is more commonly known as learning a decision tree from data and the type of tree we are describing is called a classification tree. Some of the advantages of these models include that they are simple to understand and interpret and even small data sets can be used to determine important features. But the disadvantage to the simple approach is that its classification accuracy can sometimes leave something to be desired. This is where variations on decision trees like random forests can come in to help address this problem.

So we can see that the whole idea behind the random forest model can be boiled down to taking into consideration the combination of many different possible decisions with the data to find the best option. This is why the random forest approach has been described as taking the wisdom of the crowd. One of the most popular analogy for describing the value of random forest modeling is comparing it to a financial investment strategy. Just like with the random forest model that takes many possible decision trees into account to make good final decisions with all of the data, a great investment strategy is often to diversify in many unrelated investments like gold, stocks, and bonds in order to form an overall portfolio that is more successful on average than any of the individual investments on their own. In this analogy, we consider the individual investments as the trees and the overall investment portfolio as the forest or or the ensemble of the trees. Just like an individual investment can be overly sensitive to various market forces compared to a diversified portfolio, a single tree can also be overly sensitive to small changes in the training data than the overall forest. So again, in a similar way, each of the trees in a random forest is created by a random sampling of a subset of features from the data set. This creates an ability to generate a large variety of possible trees from the data. And that is a good thing because now you can create a diverse portfolio of prediction trees to make the random for forest. So when all the trees are combined together either with averaging or majority voting, the resulting random forest will be better and more accurate than any single tree. And again while for any data some trees in the forest may be bad classifiers, but that's okay because many others will be good classifiers. So the ensemble will be a more accurate model overall.

---

## 7. Supervised Machine Learning Approaches Regression and the No Free Lunch Theorem.mp4

### Summary

This text serves as an overview of the diverse landscape of **supervised learning**, moving from basic statistical tools like **linear and logistic regression** to complex structures like **neural networks**. The author introduces the **No Free Lunch Theorem**, which posits that no single algorithm is universally superior because performance is entirely dependent on the specific **size and structure of the dataset**. To address various data challenges, the passage highlights the use of **polynomial regression** for non-linear patterns and **regularization methods** to manage complex feature dependencies. Ultimately, the source emphasizes that finding the most effective model often requires **trial and error** to determine which mathematical function best captures the relationship between features and labels.

**Keywords:** Supervised machine learning, Traditional algorithms, No free lunch, Regression analysis, Regularization methods

### Content

All right, I know we've covered a lot, but really we've just done a gentle introduction of some of the basic terms and concepts behind most of supervised learning. Before we get into neural networks, we're going to discuss more of the traditional algorithms used in machine learning. They're going to help you get a better grasp on some of these principles. But as we briefly mentioned already, you're probably aware that and you can already imagine that there are many possible approaches or algorithms that can be used to arrive at a successful model or function based on your given data set. Some of those include things that you've already heard of like logistic regress regression for categorical data and linear regression for numerical data. But there are some others that you may not know as well like decision trees support vector machines and of course neural networks. There are literally dozens of algorithms that can perform supervised and unsupervised machine learning tasks as well as many other forms along the spectrum. But for now we will run through a few examples and will not spend too much time here beyond the introductory principles. There are innumerable other resources for a deeper dive into these and frankly any other topic we've covered already. No matter what you will always be faced with the fact that there is nearly an infinite number of algorithms to choose from and there is a well-worn phrase to describe this known as the no free lunch theorem. This theorem basically states that there is no one machine learning algorithm that's best for all problems. The performance of different algorithms strongly depends on the size and structure of your data set. Thus, the correct choice of algorithm often remains unclear unless we test out our algorithms directly through plain old trial and error. And we'll discuss this strategy of course as well in later modules.

There are many different machine learning methods to be aware of and they each use different assumptions and mathematical frameworks for how data is ingested and how learning occurs within the algorithm. Regression analyses such as linear and logistic regression are some of the simplest approaches and many users of these algorithms distinguish them from the more traditionally defined machine learning methods such as random forest, support vector machines and neural networks etc. They're also fundamental for anyone who has experience in statistics or data analysis because regression is concerned with modeling the relationship between features based on a measure of error for each prediction and we'll cover the notion of error soon. We're going to start with a discussion of this in our consideration of machine learning methods.

Straight lines that provide the best fit as we saw before in our example are a form of regression. And though simple, there are many different variations of regression algorithms as well. Many of them depend on how the error is calculated. But what about if a regression line won't work well enough? What happens when the best fit of the data labels is not a straight line but instead a curve. To solve this you can use polomial regression. This is one of many sub techniques in the regression category. In this case it's used to handle nonlinearly separable data where the best fit line is not a straight line is instead a curve that fits into these data points.

All of these regression methods have tweaks that are known as regularization methods. These techniques are mathematical changes to the approach to better accommodate data set challenges such as having a very large number of features and fe that have a lot of codependencies. Some of the names of these special versions of regression are lasso, ridge, and elastic net regression. The key here is that at the core, there is a function that can discriminate and be adjusted to better fit the relationships between the features to predict the correct label. This is the core idea behind all of these models.

---

## 8. Support Vector Machine (SVM).mp4

### Summary

Support vector machines function as a powerful tool for classification by identifying an optimal boundary, or **hyperplane**, that separates different categories of data. Unlike standard regression models that account for every data point equally, an SVM focuses specifically on the **points closest to the opposing class** to create a balanced and fair division. This unique approach provides a significant advantage in **generalizability**, as the model is less likely to be skewed by unusual **statistical outliers**. By prioritizing the core boundaries of a dataset rather than its anomalies, this algorithm proves exceptionally robust for complex, **multi-dimensional medical data** where accuracy on future, unseen cases is critical.

**Keywords:** Support Vector Machines, Binary classification, Model generalizability, Outlier handling, Hyperplane optimization

### Content

So, we've talked so far about regression and decision trees. Support vector machines or SVMs is another supervised learning machine learning algorithm that's used for classification problems similar to logistic regression. That's a more powerful workforce in the toolbox of many machine learning engineers. Remember when we talked about classification in a data set with a label of pneumonia and two features in our data set of white blood cell count and temperature? We were able to create a model or a function as a line that was able to separate our data sets into pneumonia yes and pneumonia no. Given a binary classification problem, our goal is to find the best line that can classify unseen points correctly. But how you define this notion of best can result in a different model for an SVM than you would get with a logistic regression that we discussed earlier.

Note, we haven't covered exactly how we can determine if the line or function or model is a good one or not yet, which we will do, but suffice it to say that you could notice that there are several different lines with slightly different slopes or angles that could still discriminate between those two classes. Support vector machines use different measures of determining the best line or best fit. And you can think of this as in some sense trying to make the discrimination more fair between the two points. Graphically, that might look like this, where the SVM has a more balanced line than the logistic regression line. So, how does this work? Well, of course, there is some serious math involved, but it's a little easier to grasp when we keep it simple with this two-dimensional data set and a graphical representation. In this way, you can see that the SVM is more heavily considering the data points in each labeling class, pneumonia, yes or no, that are closest to one another and then creating a line or hyper plane in general for that multi-dimensional data example that is most even for these points rather than considering the entire group equally on each side like regression does.

So when would this be advantageous? Well, it can be helpful, for example, if you had a data set that included a few crazy outliers, but you didn't want that to affect your model too much. so that it would be able to work well when these crazy outliers weren't in the data set. In other words, this brings up the consideration of generalizability, which is an important concept that we'll explore in a lot more detail later. Notice how in this example, the logistic regression line is actually getting the discrimination better between the two labels compared to the SVM model that misses the outlier. So, which line, in other words, which function or train model is better for use in new data sets? Well, it depends. This seemingly better result on our historical data with the outlier by regression doesn't mean that regression will be the best model for our future use compared to the SVM model. Why? Well, imagine that this outlier is a statistical anomaly like one in 10,00. And the vast majority of the data points will fall closer into the groupings in the plot. In this case, the model that is best equipped to handle the discrimination is the one that considers more heavily the points closest to each other between the two groups. In other words, the SVM. In the case of the regression model, you can see how it might be possible for it to have been skewed such that new data points will be more easily mispredicted. Again, here we can note that the new data points are in the main part of the groupings, but the regression line because it considered the outliers in the training examples will end up mispredicting these and be a less accurate model in the end.

So, in a very simplistic way, the SVM is great in medical applications because it tends to not be skewed by outliers, which of course happen all the time in medicine. In the model, building process. But in general, SVM is great in multi-dimensional data sets where there is a multitude of features to consider.

---

## 9. Unsupervised Machine Learning.mp4

### Summary

This source explores **unsupervised machine learning**, a method used to uncover **hidden structures within unlabeled data** by grouping information based on shared characteristics rather than pre-defined outcomes. By focusing on **clustering algorithms**, the text illustrates how these models can independently identify **novel classifications and risk stratifications** that may be invisible to human observers, even helping to correct errors in existing labels. A specific medical case study on heart failure demonstrates that these **naturally occurring patterns** are medically significant when they can be **successfully replicated across different datasets** to predict patient outcomes. Ultimately, the material serves as a conceptual bridge, transitioning from traditional learning techniques to more complex topics like **neural networks and deep learning**.

**Keywords:** Unsupervised machine learning, Data clustering, Unlabeled data sets, Pattern recognition, Clinical risk stratification

### Content

So, we've introduced you to supervised machine learning, but let's try to cover some somewhat less commonly used machine learning techniques, which via the contrast will help to round out your understanding of supervised learning and how other data sets and approaches might be used in different circumstances. So, what could you do if you had a large data set but no labels at all and no feasible way to label them on your own? How would you train a function or a model to learn the pattern from your data? Take a look at our simple model from before. If we don't know which patients have pneumonia and which do not. This line doesn't have much meaning. Finding a function to separate unlabeled points isn't helpful. We still end up with examples of the same color on both sides of a line. Well, of course, you wouldn't be able to train it in the way that we had talked about with supervised machine learning because you don't have any labels. So instead, with unsupervised learning, the task is to try to find patterns based on the structure of the data as opposed to the predetermined labels. One common way to do this is grouping similar items together to form clusters. Unsupervised learning seeks to examine a collection of unlabeled examples and then group them by some notion of shared commonality. Clustering is one of the common unsupervised learning tasks. We can use clustering to define the label or the category. There are specific algorithms that are used for unsupervised learning and sometimes they're applied even to large labelled data sets for exploratory purposes and can help identify structure and substructure in the data that may not be represented by the assigned label. And this can sometimes uncover new information. Even these models can also help with spotting labeling mistakes or inconsistencies.

There are several examples in medical applications where unsupervised learning has been used, especially clustering techniques, and results in actually new clinically useful knowledge. For example, unsupervised learning has been used to examine large unlabeled data sets of electronic health record data on patients with heart failure with preserved ejection fraction in order to try to define and characterize mutually exclusive groups. making up a novel classification for treatment and prognosis. Analysis classified study participants into three distinct groups that differed marketkedly in clinical characteristics, cardiac structure and function, invasive hemodynamics and outcomes. Phenotype classification, including its ability to stratify risk, was successfully replicated in a prospective validation cohort. So the cool thing here is that the predictive ability, risk stratification, and the prospective validation that was developed out of a large data set of un label data. Note that the researchers did not label the data with three distinct categories, but the model naturally found a function based on the clustering of the data into important outcomes and was able to find three distinct categories that helped to predict the survival of these patients and important clinical measures. In data that were not used to learn the groups, application of the grouping scheme sorted patients into high, medium, and low risk of subsequent mortality. In unsupervised learning, the difficulty lies not in obtaining the grouping, although many techniques similarly suffer from small data sets, but in evaluating it or determining whether the grouping that it's finding is actually meaningful. This is because when given a data set and a clustering algorithm, we can almost always get some kind of a grouping just like we can all see patterns in the clouds. The challenge then is whether the presence of the groups or clusters or learning that a new patient is deemed a member of a certain group is actionable in the form of offering different treatment options or making a clinical decision. But most often the ability to reproduce the same groups in another data set is considered the best sign that the groups are medically meaningful and perhaps they should be managed differently. If the fact that a new record belongs to a certain group allows an assignment of a higher or lower risk of specific outcomes, that is considered a sign that the learning groups actually have meaning.

Hopefully by now you've started to feel comfortable with some of the more traditional supervised learning concepts and models. And we've also touched on the difference between supervised and unsupervised learning. These concepts may not be new to you, but whether they were review or not, we intentionally framed the discussion in a way that'll set you up for the content that you've all been waiting for, neural networks and deep learning. So, with that, let's move on to the next section.

---

